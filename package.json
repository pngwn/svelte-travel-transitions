{
  "name": "svelte-travel-transitions",
  "description": "A clone of Sarah Drasner's Vue/Nuxt page-transitions-travelapp built with SvelteKit.",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "build:ci": "npx pnpm install --store=node_modules/.pnpm-store && vite build",
    "preview": "vite preview",
    "prebuild": "test \"$CI\" = true && npx pnpm install --store=node_modules/.pnpm-store || echo skiping pnpm install"
  },
  "dependencies": {
    "animejs": "^2.2.0",
    "immer": "^1.9.3"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "sass": "^1.32.13",
    "svelte": "^3.38.2",
    "svelte-preprocess": "^4.10.6",
    "vite": "^3.0.4"
  }
}
